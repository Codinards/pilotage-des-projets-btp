{% extends 'base.html.twig' %}

{% block title %}{{ __u('creation') }} {{ __u('new sector') }}{% endblock %}

{% block body %}
<h1>{{ __u('creation') }} {{ __u('new sector') }} </h1>
<div class="bg-dark">
    <hr>
    <hr>
</div>
<a href="{{ path('app_sector_index', {_locale: app.request.locale ?? app.request.defaultLocale}) }}" class="btn btn-info">{{ __('back to list') }}</a>
<hr>
{{ include('sector/_form.html.twig') }}

{% endblock %}