{% extends 'base.html.twig' %}

{% block title %}{{ __u('Creation') }} {{ __u('new type') }}{% endblock %}

{% block body %}
<h1>{{ __u('Creation') }} {{ __u('new type') }} </h1>
<div class="bg-dark">
    <hr>
    <hr>
</div>
<a href="{{ path('app_project_type_index', {_locale: app.request.locale ?? app.request.defaultLocale}) }}" class="btn btn-info">{{ __('back to list') }}</a>
<hr>
{{ include('project_type/_form.html.twig') }}

{% endblock %}