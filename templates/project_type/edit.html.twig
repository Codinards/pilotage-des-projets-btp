{% extends 'base.html.twig' %}

{% block title %}{{ __u('type edition') | title }}{% endblock %}

{% block body %}
<h1>{{ __u('type edition') | title }} | <span class="text-info">{{ project_type }}</span></h1>
<div class="bg-dark">
    <hr>
    <hr>
</div>
<div class="row">
    <div class="col-6">
        <a href="{{ path('app_project_type_index', {_locale: app.request.locale ?? app.request.defaultLocale}) }}" class="btn btn-info">{{ __('back to list') }}</a>
    </div>
    <div class="col-6 text-right">
        {{ include('project_type/_delete_form.html.twig') }}
    </div>
</div>
<hr>
{{ include('project_type/_form.html.twig', {'button_label': 'save'}) }}

{% endblock %}