{% extends 'base.html.twig' %}

{% block title %}{{ __t('type index') | title }}{% endblock %}

{% block body %}
<h1>{{ __t('type index') | title }}</h1>

<div class="bg-dark">
    <hr>
    <hr>
</div>
{% include "alert.html.twig" %}
<div class="row">
    <div class="col text-right">
        <a href="{{ path('app_project_type_new', {_locale: app.request.locale ?? app.request.defaultLocale}) }}" class="btn btn-primary">{{ __u('new type') }}</a>
        <a href="{{ path('app_project_type_index', {_locale: app.request.locale ?? app.request.defaultLocale, pdf: true}) }}" class="btn btn-success" style="min-width: 100px;">{{ __u('to PDF')  }}</a>
    </div>
</div>
<hr>
<table class="table">
    <thead class="bg-secondary">
        <tr>
                            <th>{{ __u('Id') }}</th>
                            <th>{{ __u('Name') }}</th>
                        <th>{{ __('actions') }}</th>
        </tr>
    </thead>
    <tbody>
        {% for project_type in project_types %}
        <tr>
                            <td>{{ loop.index }}</td>
                            <td>{{ project_type.name }}</td>
                        <td>
                {# <a href="{{ path('app_project_type_show', {'id': project_type.id, _locale: app.request.locale ?? app.request.defaultLocale}) }}" class="btn btn-info">{{ __('show') }}</a> #}
                <a href="{{ path('app_project_type_edit', {'id': project_type.id, _locale: app.request.locale ?? app.request.defaultLocale}) }}" class="btn btn-success">{{ __('edit') }}</a>
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}